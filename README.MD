# Crop-Shop-N-GO
Shop for items using a reverse image search computer app. Mobile app coming soon!

## Build (Gradle)

- **Compile:** `./gradlew build` (or `gradle build` if Gradle is installed)
- **Run:** `./gradlew run` (or `gradle run`)
- **JAR:** `./gradlew jar` — output in `build/libs/CropShopNGo-1.0.jar` (run with `java -Djava.library.path=lib -jar build/libs/CropShopNGo-1.0.jar` and ensure `lib/` is on the classpath or next to the JAR)
